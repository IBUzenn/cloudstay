# Member 3 — Evidence Folder

This folder stores execution evidence for Member 3's database administration and data engineering work.

## Expected Evidence Files

Please place actual SQL logs or ERD diagrams here upon completing your tasks:

1. `schema-execution.log` — Terminal output from executing `mysql -u root -p < database/schema.sql`.
2. `tables-structure.txt` — Output of `SHOW TABLES;` and `DESCRIBE bookings;` from MySQL CLI.
3. `views-verification.txt` — Output of querying `v_room_availability` and `v_booking_summary`.
4. `trigger-test.log` — SQL verification log showing `hostels.total_rooms` updating on room insert/delete.
5. `er-diagram-export.png` — Rendered ERD diagram matching `docs/design/er-diagram.md`.

> **Note:** Do NOT upload mock or fake logs. Store actual execution output captured during local database setup.
