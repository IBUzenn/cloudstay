# Member 3 — Completion Checklist

Use this checklist to track your progress and verify completion before submitting your work to the project coordinator.

- [ ] Environment configured (MySQL 8.0 server installed & running)
- [ ] Task `M3-DB-001` complete (`schema.sql` 3NF table DDL executed cleanly)
- [ ] Task `M3-DB-002` complete (FK indices & `total_rooms` triggers active)
- [ ] Task `M3-DB-003` complete (`v_room_availability` & `v_booking_summary` views created)
- [ ] Task `M3-DB-004` complete (`backend/src/config/database.js` mysql2 pool configured)
- [ ] Task `M3-DB-005` complete (`seeds.sql` & `procedures.sql` executed without errors)
- [ ] Schema DDL verified via `SHOW TABLES;` and `DESCRIBE bookings;`
- [ ] ERD documentation updated (`docs/design/er-diagram.md`)
- [ ] SQL execution logs / ERD exports stored in `evidence/` directory
- [ ] `CONTRIBUTION.md` updated with actual completed tasks and commit hashes
- [ ] Changes committed to `feature/database-schema` branch
- [ ] Pull Request opened for team review
