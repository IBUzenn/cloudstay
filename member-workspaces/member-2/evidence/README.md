# Member 2 — Evidence Folder

This folder stores execution evidence for Member 2's backend and security work.

## Expected Evidence Files

Please place actual API test output or Postman exports here upon completing your tasks:

1. `api-health-check.json` — Response payload from `GET /api/health`.
2. `auth-flow-test.log` — Terminal output showing registration, login, JWT token issuance, and logout token revocation.
3. `booking-transaction-test.log` — Test output verifying `SELECT ... FOR UPDATE` locking prevents concurrent double-booking.
4. `postman-collection.json` — Postman collection or HTTP client test suite exporting all verified backend endpoints.
5. `security-audit-results.txt` — Inspection report verifying `helmet` HTTP security headers and rate limiter enforcement.

> **Note:** Do NOT upload mock or fake logs. Store actual execution output captured during local testing.
